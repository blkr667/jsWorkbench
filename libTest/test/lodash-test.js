var _    = require("lodash");
var expect    = require("chai").expect;

describe("lodash check", function() {
  describe("lodash check", function() {
    it("lodash check", function() {

      
      var currentDate = _.now();


      expect(currentDate).to.not.equal(undefined);
    });
  });


});